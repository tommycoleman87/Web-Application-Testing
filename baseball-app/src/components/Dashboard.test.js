import React from "react";
import { render } from "@testing-library/react";
import "@testing-library/react/cleanup-after-each";
import Dashboard from './Dashboard';
/*
describe('<Dashboard />', () => {
    it('should increment strikes by 1 when getting a strike', () => {
        const value = '';
        const valueConditional = (num) => {

        }
    })

    it('should increment balls by 1 when getting a ball', () => {

    })

    it('should increment strikes by 1 when getting a foul', () => {

    })

    it('should reset balls and strikes to 0 when a strike is hit at 2 strikes', () => {

    })

    it('should reset balls and strikes to 0 when a ball is hit at 3 balls', () =>{

    })

    it('should only increase strikes up to 2 when getting a foul', () =>{

    })

    it('should reset balls and strikes to 0 when a hit occurs', () => {

    })
})*/